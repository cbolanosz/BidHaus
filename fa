[1mdiff --git a/auctions/forms.py b/auctions/forms.py[m
[1mindex 80a6682..a27bf69 100644[m
[1m--- a/auctions/forms.py[m
[1m+++ b/auctions/forms.py[m
[36m@@ -11,13 +11,50 @@[m [mUser = get_user_model()[m
 DATETIME_LOCAL_FORMAT = "%Y-%m-%dT%H:%M"[m
 [m
 [m
[32m+[m[32mclass MultipleFileInput(forms.ClearableFileInput):[m
[32m+[m[32m    """The file input Django renders for one file, allowed to take several."""[m
[32m+[m
[32m+[m[32m    allow_multiple_selected = True[m
[32m+[m
[32m+[m
[32m+[m[32mclass MultipleImageField(forms.ImageField):[m
[32m+[m[32m    """An image field that validates every file chosen in a single input."""[m
[32m+[m
[32m+[m[32m    def __init__(self, *args, **kwargs):[m
[32m+[m[32m        kwargs.setdefault("widget", MultipleFileInput(attrs={"multiple": True, "accept": "image/*"}))[m
[32m+[m[32m        kwargs.setdefault("validators", [validate_photograph_size])[m
[32m+[m[32m        super().__init__(*args, **kwargs)[m
[32m+[m
[32m+[m[32m    def clean(self, data, initial=None):[m
[32m+[m[32m        """Validate every chosen file. An empty selection is validated as one[m
[32m+[m[32m        missing value, so a required field still reports that it is empty."""[m
[32m+[m[32m        clean_one_image = super().clean[m
[32m+[m[32m        if not isinstance(data, (list, tuple)):[m
[32m+[m[32m            data = [data][m
[32m+[m[32m        if not data:[m
[32m+[m[32m            data = [None][m
[32m+[m[32m        return [clean_one_image(image, initial) for image in data][m
[32m+[m
[32m+[m
 class AuctionForm(forms.ModelForm):[m
     """Data a seller fills in to publish an auction (FR01).[m
 [m
[32m+[m[32m    The first photographs are asked for here so that no auction is ever stored[m
[32m+[m[32m    without one, which is what DBR03 requires. The rest, up to MAX_PHOTOGRAPHS,[m
[32m+[m[32m    are added afterwards on the photograph page.[m
[32m+[m
     The seller is a visible field because sprint 1 has no login yet; it will be[m
     taken from the session once FR31 is implemented.[m
     """[m
 [m
[32m+[m[32m    images = MultipleImageField([m
[32m+[m[32m        label="Fotografías",[m
[32m+[m[32m        help_text=([m
[32m+[m[32m            f"Al menos {MIN_PHOTOGRAPHS}, de máximo {MAX_PHOTOGRAPH_SIZE_MB} MB cada una. "[m
[32m+[m[32m            f"Después podrás añadir el resto, hasta {MAX_PHOTOGRAPHS}."[m
[32m+[m[32m        ),[m
[32m+[m[32m    )[m
[32m+[m
     class Meta:[m
         model = Auction[m
         fields = ["seller", "category", "title", "description", "condition", "starting_price", "closing_date"][m
[36m@@ -71,36 +108,11 @@[m [mclass AuctionSearchForm(forms.Form):[m
         return cleaned_data[m
 [m
 [m
[31m-class MultipleFileInput(forms.ClearableFileInput):[m
[31m-    """The file input Django renders for one file, allowed to take several."""[m
[31m-[m
[31m-    allow_multiple_selected = True[m
[31m-[m
[31m-[m
[31m-class MultipleImageField(forms.ImageField):[m
[31m-    """An image field that validates every file chosen in a single input."""[m
[31m-[m
[31m-    def __init__(self, *args, **kwargs):[m
[31m-        kwargs.setdefault("widget", MultipleFileInput(attrs={"multiple": True, "accept": "image/*"}))[m
[31m-        super().__init__(*args, **kwargs)[m
[31m-[m
[31m-    def clean(self, data, initial=None):[m
[31m-        """Validate every chosen file. An empty selection is validated as one[m
[31m-        missing value, so a required field still reports that it is empty."""[m
[31m-        clean_one_image = super().clean[m
[31m-        if not isinstance(data, (list, tuple)):[m
[31m-            data = [data][m
[31m-        if not data:[m
[31m-            data = [None][m
[31m-        return [clean_one_image(image, initial) for image in data][m
[31m-[m
[31m-[m
 class PhotographUploadForm(forms.Form):[m
     """Photographs a seller attaches to an auction that already exists (FR02)."""[m
 [m
     images = MultipleImageField([m
         label="Fotografías",[m
[31m-        validators=[validate_photograph_size],[m
         help_text=([m
             f"Entre {MIN_PHOTOGRAPHS} y {MAX_PHOTOGRAPHS} imágenes, "[m
             f"de máximo {MAX_PHOTOGRAPH_SIZE_MB} MB cada una."[m
[1mdiff --git a/auctions/services.py b/auctions/services.py[m
[1mindex 48f481c..7d72eb1 100644[m
[1m--- a/auctions/services.py[m
[1m+++ b/auctions/services.py[m
[36m@@ -13,10 +13,14 @@[m [mFIRST_DISPLAY_ORDER = 1[m
 [m
 [m
 @transaction.atomic[m
[31m-def publish_auction(seller, category, title, description, condition, starting_price, closing_date):[m
[31m-    """Publish an auction that opens at its starting price (FR01).[m
[31m-[m
[31m-    Only a user registered with the seller role may publish. The current price[m
[32m+[m[32mdef publish_auction([m
[32m+[m[32m    seller, category, title, description, condition, starting_price, closing_date, images[m
[32m+[m[32m):[m
[32m+[m[32m    """Publish an auction with its first photographs (FR01).[m
[32m+[m
[32m+[m[32m    Only a user registered with the seller role may publish. The auction and its[m
[32m+[m[32m    photographs are stored in the same transaction, so an auction never reaches[m
[32m+[m[32m    the catalogue without the photograph that DBR03 demands. The current price[m
     starts equal to the starting price, so the first bid is compared against a[m
     real amount instead of against a null.[m
     """[m
[36m@@ -36,6 +40,7 @@[m [mdef publish_auction(seller, category, title, description, condition, starting_pr[m
     )[m
     auction.full_clean()[m
     auction.save()[m
[32m+[m[32m    add_photographs(auction, images)[m
     return auction[m
 [m
 [m
[1mdiff --git a/auctions/tests/test_auction_create_view.py b/auctions/tests/test_auction_create_view.py[m
[1mindex 93f5a9a..3101872 100644[m
[1m--- a/auctions/tests/test_auction_create_view.py[m
[1m+++ b/auctions/tests/test_auction_create_view.py[m
[36m@@ -1,26 +1,24 @@[m
 """Tests of the publication page (FR01)."""[m
 [m
[32m+[m[32mimport tempfile[m
 from datetime import timedelta[m
 [m
 from django.contrib.auth import get_user_model[m
[31m-from django.test import TestCase[m
[32m+[m[32mfrom django.test import TestCase, override_settings[m
 from django.urls import reverse[m
 from django.utils import timezone[m
 [m
 from auctions.forms import DATETIME_LOCAL_FORMAT[m
[31m-from auctions.models import Auction, Category[m
[32m+[m[32mfrom auctions.models import MAX_PHOTOGRAPHS, Auction, Category[m
[32m+[m[32mfrom auctions.tests.factories import build_image, create_seller[m
 [m
 User = get_user_model()[m
 [m
 [m
[32m+[m[32m@override_settings(MEDIA_ROOT=tempfile.mkdtemp())[m
 class AuctionCreateViewTests(TestCase):[m
     def setUp(self):[m
[31m-        self.seller = User.objects.create_user([m
[31m-            email="vendedor@bidhaus.co",[m
[31m-            password="clave-de-prueba",[m
[31m-            full_name="Vendedora de prueba",[m
[31m-            role=User.Role.SELLER,[m
[31m-        )[m
[32m+[m[32m        self.seller = create_seller()[m
         self.category = Category.objects.create(name="Fotografía")[m
         self.url = reverse("auctions:auction_create")[m
 [m
[36m@@ -34,6 +32,7 @@[m [mclass AuctionCreateViewTests(TestCase):[m
             "condition": Auction.Condition.USED,[m
             "starting_price": "250000",[m
             "closing_date": closing_date.strftime(DATETIME_LOCAL_FORMAT),[m
[32m+[m[32m            "images": [build_image()],[m
         }[m
         payload.update(overrides)[m
         return self.client.post(self.url, payload)[m
[36m@@ -52,6 +51,25 @@[m [mclass AuctionCreateViewTests(TestCase):[m
             response, reverse("auctions:auction_photographs", args=[auction.pk])[m
         )[m
 [m
[32m+[m[32m    def test_stores_the_photograph_that_came_with_the_form(self):[m
[32m+[m[32m        self.post(images=[build_image(), build_image()])[m
[32m+[m
[32m+[m[32m        self.assertEqual(Auction.objects.get().photographs.count(), 2)[m
[32m+[m
[32m+[m[32m    def test_requires_at_least_one_photograph(self):[m
[32m+[m[32m        response = self.post(images=[])[m
[32m+[m
[32m+[m[32m        self.assertEqual(Auction.objects.count(), 0)[m
[32m+[m[32m        self.assertContains(response, "Este campo es obligatorio.")[m
[32m+[m
[32m+[m[32m    def test_reports_more_photographs_than_an_auction_admits(self):[m
[32m+[m[32m        images = [build_image() for _ in range(MAX_PHOTOGRAPHS + 1)][m
[32m+[m
[32m+[m[32m        response = self.post(images=images)[m
[32m+[m
[32m+[m[32m        self.assertEqual(Auction.objects.count(), 0)[m
[32m+[m[32m        self.assertContains(response, "admite 8 fotografías como máximo")[m
[32m+[m
     def test_offers_only_registered_sellers_as_authors(self):[m
         bidder = User.objects.create_user([m
             email="comprador@bidhaus.co",[m
[1mdiff --git a/auctions/tests/test_publish_auction.py b/auctions/tests/test_publish_auction.py[m
[1mindex a48242a..df2dfb7 100644[m
[1m--- a/auctions/tests/test_publish_auction.py[m
[1m+++ b/auctions/tests/test_publish_auction.py[m
[36m@@ -1,28 +1,26 @@[m
 """Tests of the publication use case (FR01)."""[m
 [m
[32m+[m[32mimport tempfile[m
 from datetime import timedelta[m
 from decimal import Decimal[m
 [m
 from django.contrib.auth import get_user_model[m
 from django.core.exceptions import ValidationError[m
[31m-from django.test import TestCase[m
[32m+[m[32mfrom django.test import TestCase, override_settings[m
 from django.utils import timezone[m
 [m
[31m-from auctions.exceptions import NotASeller[m
[32m+[m[32mfrom auctions.exceptions import NoPhotographs, NotASeller[m
 from auctions.models import Auction, Category[m
 from auctions.services import publish_auction[m
[32m+[m[32mfrom auctions.tests.factories import build_image, create_seller[m
 [m
 User = get_user_model()[m
 [m
 [m
[32m+[m[32m@override_settings(MEDIA_ROOT=tempfile.mkdtemp())[m
 class PublishAuctionTests(TestCase):[m
     def setUp(self):[m
[31m-        self.seller = User.objects.create_user([m
[31m-            email="vendedor@bidhaus.co",[m
[31m-            password="clave-de-prueba",[m
[31m-            full_name="Vendedora de prueba",[m
[31m-            role=User.Role.SELLER,[m
[31m-        )[m
[32m+[m[32m        self.seller = create_seller()[m
         self.category = Category.objects.create(name="Coleccionables")[m
         self.closing_date = timezone.now() + timedelta(days=3)[m
 [m
[36m@@ -35,6 +33,7 @@[m [mclass PublishAuctionTests(TestCase):[m
             "condition": Auction.Condition.USED,[m
             "starting_price": Decimal("250000.00"),[m
             "closing_date": self.closing_date,[m
[32m+[m[32m            "images": [build_image()],[m
         }[m
         arguments.update(overrides)[m
         return publish_auction(**arguments)[m
[36m@@ -50,6 +49,17 @@[m [mclass PublishAuctionTests(TestCase):[m
 [m
         self.assertEqual(auction.current_price, Decimal("80000.00"))[m
 [m
[32m+[m[32m    def test_stores_the_first_photographs_with_the_auction(self):[m
[32m+[m[32m        auction = self.publish(images=[build_image(), build_image()])[m
[32m+[m
[32m+[m[32m        self.assertEqual(auction.photographs.count(), 2)[m
[32m+[m
[32m+[m[32m    def test_rejects_an_auction_without_photographs(self):[m
[32m+[m[32m        with self.assertRaises(NoPhotographs):[m
[32m+[m[32m            self.publish(images=[])[m
[32m+[m
[32m+[m[32m        self.assertEqual(Auction.objects.count(), 0)[m
[32m+[m
     def test_rejects_a_closing_date_already_in_the_past(self):[m
         past_date = timezone.now() - timedelta(hours=1)[m
 [m
[1mdiff --git a/auctions/views.py b/auctions/views.py[m
[1mindex 37bf806..35e4f9d 100644[m
[1m--- a/auctions/views.py[m
[1m+++ b/auctions/views.py[m
[36m@@ -38,7 +38,7 @@[m [mdef auction_create(request):[m
     if request.method != "POST":[m
         return render(request, "auctions/auction_form.html", {"form": AuctionForm()})[m
 [m
[31m-    form = AuctionForm(request.POST)[m
[32m+[m[32m    form = AuctionForm(request.POST, request.FILES)[m
     if not form.is_valid():[m
         return render(request, "auctions/auction_form.html", {"form": form})[m
 [m
[36m@@ -51,12 +51,19 @@[m [mdef auction_create(request):[m
             condition=form.cleaned_data["condition"],[m
             starting_price=form.cleaned_data["starting_price"],[m
             closing_date=form.cleaned_data["closing_date"],[m
[32m+[m[32m            images=form.cleaned_data["images"],[m
         )[m
     except NotASeller:[m
         form.add_error("seller", "Solo un vendedor registrado puede publicar una subasta.")[m
         return render(request, "auctions/auction_form.html", {"form": form})[m
[32m+[m[32m    except TooManyPhotographs:[m
[32m+[m[32m        form.add_error("images", f"Una subasta admite {MAX_PHOTOGRAPHS} fotografías como máximo.")[m
[32m+[m[32m        return render(request, "auctions/auction_form.html", {"form": form})[m
 [m
[31m-    messages.success(request, f"Se publicó la subasta «{auction.title}». Ahora añade sus fotografías.")[m
[32m+[m[32m    messages.success([m
[32m+[m[32m        request,[m
[32m+[m[32m        f"Se publicó la subasta «{auction.title}». Puedes añadir más fotografías.",[m
[32m+[m[32m    )[m
     return redirect("auctions:auction_photographs", auction_id=auction.pk)[m
 [m
 [m
[1mdiff --git a/templates/auctions/auction_form.html b/templates/auctions/auction_form.html[m
[1mindex ee1f3cb..5cf88d5 100644[m
[1m--- a/templates/auctions/auction_form.html[m
[1m+++ b/templates/auctions/auction_form.html[m
[36m@@ -5,7 +5,7 @@[m
 {% block content %}[m
 <h1>Publicar una subasta</h1>[m
 [m
[31m-<form method="post" class="form" novalidate>[m
[32m+[m[32m<form method="post" enctype="multipart/form-data" class="form" novalidate>[m
     {% csrf_token %}[m
 [m
     {% if form.non_field_errors %}[m
